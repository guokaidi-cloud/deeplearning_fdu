# YOLOv8 人脸检测系统 🎯

基于 YOLOv8 和 OpenCV 实现的高效人脸检测系统，支持实时视频检测、批量图像处理等功能。

## 🚀 快速开始

### 基本用法

```bash
python opencv_face_detector.py --input video.mp4 --output result.mp4
```

### 参数说明

| 参数 | 说明 | 示例 |
|------|------|------|
| `--input` | 输入视频文件路径 | `video.mp4` |
| `--output` | 输出视频文件路径 | `result.mp4` |
| `--start-time` | 开始时间 (秒数或 HH:MM:SS 格式) | `3600` 或 `01:00:00` |
| `--end-time` | 结束时间 (秒数或 HH:MM:SS 格式) | `7200` 或 `02:00:00` |
| `--max-frames` | 最大处理帧数 (用于测试) | `1000` |
| `--min-neighbors` | 最小邻居数 | `3` |
| `--min-size` | 最小人脸尺寸 [width height] | `15 15` |

## 📖 使用示例

### 从视频中间开始检测

python yolov8_face_detection/opencv_face_detector.py \
 --input "教室学生机位/深度学习应用++2025-11-22第1-3节+[30657+-+524562]教室流.mp4" \
 --output "test_data/classroom_1hour_later.mp4" \
 --start-time "3600" --max-frames 1000 --min-neighbors 3 --min-size 15 15